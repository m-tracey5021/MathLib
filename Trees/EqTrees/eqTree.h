#pragma once

#include <string>

using namespace std;

class EqTree {
    private:

    public:
        int* parseForMainOperator(string eq, int start, int end){}
        int* parseForOperands(string eq)
};